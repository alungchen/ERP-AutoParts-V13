# 正式環境：Worker 與 Pages 不同網域時設定（無結尾斜線）
# VITE_API_BASE=https://erp-autoparts-api.xxx.workers.dev

# 本機 npm run dev：預設已於 .env.development 設定 VITE_DEV_API_PROXY（轉發 /api 至 Pages）
# npm run dev:all：使用 mode local-worker，讀取 .env.local-worker，/api 走本機 8787
# 若要單獨覆寫雲端網址，可使用 .env.development.local 或 .env.local（見 .gitignore）
