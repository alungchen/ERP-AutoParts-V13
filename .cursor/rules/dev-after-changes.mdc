---
description: 程式變更後啟動開發伺服器供瀏覽器驗證
alwaysApply: true
---

# 變更後執行開發伺服器

在本專案（ERP-AutoParts-V13）完成 **實質程式修改**（新增/修改原始碼、設定檔等）後：

1. 在專案根目錄執行：`npm run dev`（長時執行，應以背景方式執行）
2. 若終端機顯示埠已被佔用（例如已有 Vite 在跑），表示開發伺服器已在運行，**無需**再啟第二個；可改為提醒使用者直接重新整理瀏覽器（HMR 通常已套用變更）
3. 僅純文件／註解微調且使用者不要求時可省略

使用 PowerShell 時以 `;` 連接指令，例如：`cd "專案根目錄"; npm run dev`
